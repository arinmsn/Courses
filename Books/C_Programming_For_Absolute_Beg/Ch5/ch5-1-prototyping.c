#include <stdio.h>

float fgetRemainder(int, int);  // function that divides two numbers and returns the remainder
float fMaxNumber(int, int);     // function that finds the larger of two numbers and returns the result
void printMenu(void);           // function that prints an ATM menu--it receives no parameters and returns no value

int main()
{
    printf("\n");
}
